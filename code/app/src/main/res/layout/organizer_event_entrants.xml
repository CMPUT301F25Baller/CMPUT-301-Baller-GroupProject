<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    tools:context=".EventEntrantsActivity">

    <!-- Simple header: back image + title -->
    <ImageButton
        android:id="@+id/backButton"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_margin="16dp"
        android:src="@drawable/ic_back_black"
        android:background="@android:color/transparent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/eventEntrantsTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="16dp"
        android:text="Event Entrants"
        android:textColor="#120D26"
        android:textSize="24sp"
        app:layout_constraintStart_toEndOf="@id/backButton"
        app:layout_constraintTop_toTopOf="@id/backButton"
        app:layout_constraintBottom_toBottomOf="@id/backButton"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Filter chip -->
    <com.google.android.material.chip.Chip
        android:id="@+id/chipFilter"
        style="@style/Widget.MaterialComponents.Chip.Choice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="12dp"
        android:text="Invited"
        android:textColor="#1D1B20"
        app:chipIcon="@drawable/ic_arrow_up"
        app:chipIconEnabled="true"
        app:chipIconSize="8dp"
        app:chipStartPadding="16dp"
        app:chipCornerRadius="8dp"
        app:chipStrokeColor="#CAC4D0"
        app:chipStrokeWidth="1dp"
        app:chipBackgroundColor="@android:color/transparent"
        app:layout_constrainedWidth="true"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/eventEntrantsTitle"/>

    <!-- List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerEntrants"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="8dp"
        android:paddingBottom="96dp"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_entrant"
        app:layout_constraintTop_toBottomOf="@id/chipFilter"
        app:layout_constraintBottom_toTopOf="@+id/inviteButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Bottom action button -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/inviteButton"
        android:layout_width="0dp"
        android:layout_height="56dp"
        android:layout_marginBottom="15dp"
        android:layout_marginStart="75dp"
        android:layout_marginEnd="75dp"
        android:paddingStart="30dp"
        android:paddingEnd="10dp"
        android:text="Invite entrant"
        android:textAllCaps="true"
        android:textColor="@color/white"
        android:textAlignment="center"
        android:gravity="center"
        app:cornerRadius="8dp"
        app:iconPadding="8dp"
        app:iconGravity="end"
        app:icon="@drawable/ic_list"
        app:iconSize="24dp"
        app:iconTint="@null"
        app:backgroundTint="#5669FF"
        app:rippleColor="@android:color/transparent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
