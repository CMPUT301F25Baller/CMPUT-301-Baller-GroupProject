<resources>
    <!-- App theme (Material 3) -->
    <style name="Theme.BallerEvents" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- keep status bar transparent; no ShapeAppearance references -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>