<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BallerEvents">

        <!-- Admin Dashboard (your new screen) -->
        <activity
            android:name=".AdminDashboardActivity"
            android:exported="false" />

        <!-- Optional: “See all” placeholders; safe to leave if you want to demo navigation -->
        <activity android:name=".AdminEventsActivity" android:exported="false" />
        <activity android:name=".AdminProfilesActivity" android:exported="false" />
        <activity android:name=".AdminImagesActivity" android:exported="false" />

        <!-- Your launcher (already present in your project) -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
